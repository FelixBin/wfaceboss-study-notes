server:
  port: 8080

spring:
  application:
    name: cloud-eureka-order-service

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka
